namespace GridNomads;

/// <summary>
/// Centralized color definitions for consistent use across the project.
/// </summary>
public static class ColorConstants
{
    public static readonly Color BackgroundColor = Color.FromRgb(30, 30, 30); // Dark gray background
    public static readonly Color TrailColor = BackgroundColor; // Trails fade to match background
    public static readonly Color RedNomadColor = Colors.OrangeRed; // High-contrast red
    public static readonly Color BlueNomadColor = Colors.DodgerBlue; // High-contrast blue
}
